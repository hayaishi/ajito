ajito
=====

```
git config --global user.name "Your Name"
git config --global user.email asyoulike@example.com
git config --global color.ui true
git config --global core.editor emacs
$ git checkout --no-track -b add-tools-txt origin/master
$ git status
$ git diff
$ git diff --staged
$ git add -i
$ git commit
$ git push -u origin
$ git show
$ git log --oneline
```
